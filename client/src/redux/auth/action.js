const initialState = {
  token: false,
  isAuthLoading: false,
  isError: false,
};

export const AuthReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
